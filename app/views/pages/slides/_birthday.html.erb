<div class="col-lg-12">
    <!--< %= @this_months_birthdays %>-->
    <!--< % if b[].strftime("%d") == Date.today.strftime("%d")%>-->
    <div class="widget-head-color-box navy-bg p-lg text-center">
        <div class="m-b-md">
            <h3 class="font-bold no-margins">
                Happy Birthday
            </h3>
            <h2 class="font-bold no-margins">Jason!</h2>
            <!--<small>< %= b.strftime("%B") %> < %= b.strftime("%d").to_i.ordinalize %></small>-->
        </div>
        <div class="col-lg-4 col-lg-offset-4">
            <img src="<%= image_path('employees/jason.jpg') %>" class="img-circle circle-border m-b-md" alt="profile">
        </div>
    </div>
    <!--< % else %>-->
    
    <!--< % end %>-->
    <!--< % end %>-->
    
</div>